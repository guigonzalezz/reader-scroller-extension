import React from "react";
import ReactDOM from "react-dom/client";
import { ReaderToolsPopup } from "@/components/ReaderToolsPopup";
import "@/styles/globals.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <ReaderToolsPopup />
  </React.StrictMode>,
);
